import { Check, CircleAlert, Clock3, MapPinned } from "lucide-react";
import type { RouteOption } from "@/types/api";

export function RouteTable({ routes, selected, onSelect }: { routes: RouteOption[]; selected: string; onSelect: (route: RouteOption) => void }) {
  return <div className="overflow-x-auto rounded-2xl border border-[#d7e8f1] bg-white"><table className="w-full min-w-[640px] text-left"><thead className="border-b border-[#e6eff4] bg-[#f8fbfd]"><tr>{["Route", "Distance", "Travel time", "Risk score", "Calculated score", "Status"].map((h) => <th key={h} className="px-4 py-3 text-[10px] font-mono uppercase tracking-[0.12em] text-[#73869f]">{h}</th>)}</tr></thead><tbody className="divide-y divide-[#edf3f6]">{routes.map((route) => { const active = selected === route.route_name; const hours = Math.floor(route.travel_time / 60); const mins = Math.round(route.travel_time % 60); const formattedTime = hours > 0 ? `${hours}h ${mins}m` : `${mins}m`; const hasRisk = route.risk_score !== null && route.risk_score !== undefined; return <tr key={route.route_name} className={`transition hover:bg-[#f8fcfd] ${active ? "bg-[#effbfe]" : ""}`}><td className="px-4 py-4"><button className="flex items-center gap-2 text-sm font-semibold text-[#102052]" onClick={() => onSelect(route)}><span className={`grid size-6 place-items-center rounded-md ${active ? "bg-[#19c8e8] text-[#07104b]" : "bg-[#e8f1f6] text-[#658098]"}`}>{active ? <Check size={14}/> : <span className="text-[10px]">{route.route_name.slice(-1)}</span>}</span><div><span>{route.route_name}</span>{route.provider && <span className="ml-2 rounded bg-slate-100 px-1.5 py-0.5 font-mono text-[9px] uppercase tracking-wider text-slate-600">{route.provider}</span>}</div></button></td><td className="px-4 py-4 font-mono text-xs text-[#4f6781]"><span className="inline-flex items-center gap-1.5"><MapPinned size={13}/> {route.distance} km</span></td><td className="px-4 py-4 font-mono text-xs text-[#4f6781]"><span className="inline-flex items-center gap-1.5"><Clock3 size={13}/> {formattedTime}</span></td><td className="px-4 py-4">{hasRisk ? <span className={`inline-flex items-center gap-1.5 rounded-full px-2 py-1 text-[10px] font-mono ${route.risk_score! > 60 ? "bg-[#fff0f1] text-[#c94359]" : route.risk_score! > 35 ? "bg-[#fff8e8] text-[#ae7713]" : "bg-[#e9fbf4] text-[#187957]"}`}><CircleAlert size={12}/> {route.risk_score}</span> : <span className="inline-flex items-center gap-1.5 rounded-full bg-slate-100 px-2 py-1 font-mono text-[10px] text-slate-500"><CircleAlert size={12}/> Risk assessment pending</span>}</td><td className="px-4 py-4 font-mono text-xs font-semibold text-[#102052]">{hasRisk ? route.calculated_score : "—"}</td><td className="px-4 py-4"><span className={`text-[10px] font-mono uppercase tracking-[0.08em] ${active ? "text-[#0a9c7a]" : "text-[#73869f]"}`}>{active ? "Recommended" : route.status}</span></td></tr>})}</tbody></table></div>;
}
